```
import logging
from typing import Union

logging.basicConfig(level=logging.INFO)

def add(a: int, b: int) -> int:
    """
    Returns the sum of two integers.

    Args:
        a (int): The first integer.
        b (int): The second integer.

    Returns:
        int: The sum of a and b.
    """
    try:
        if not isinstance(a, int) or not isinstance(b, int):
            raise ValueError("Both inputs must be integers.")
        return a + b
    except (TypeError, ValueError) as e:
        logging.error(f"{e.__class__.__name__} in add function: {e}")
    except Exception as e:
        logging.error(f"Unexpected error in add function: {e}")

def multiply(a: int, b: int) -> Union[int, float]:
    """
    Returns the product of two integers.

    Args:
        a (int): The first integer.
        b (int): The second integer.

    Returns:
        int or float: The product of a and b. If b is 0, returns 0.
    """
    try:
        if not isinstance(a, int) or not isinstance(b, int):
            raise ValueError("Both inputs must be integers.")
        result = a * b
        return result
    except TypeError as e:
        logging.error(f"TypeError in multiply function: {e}")
    except ValueError as e:
        logging.error(f"ValueError in multiply function: {e}")

def divide(a: int, b: int) -> float:
    """
    Returns the result of dividing two integers.

    Args:
        a (int): The dividend.
        b (int): The divisor.

    Returns:
        float: The result of the division. If b is 0, raises ZeroDivisionError.
    """
    try:
        if not isinstance(a, int) or not isinstance(b, int):
            raise ValueError("Both inputs must be integers.")
        if b == 0:
            raise ZeroDivisionError("Cannot divide by zero.")
        return a / b
    except (TypeError, ValueError, ZeroDivisionError) as e:
        logging.error(f"{e.__class__.__name__} in divide function: {e}")
```

